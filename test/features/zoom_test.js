'use strict';

describe('Zooming', function() {
  // before(function() {
  //   return browser.visit('/');
  // });
  //
  // before(function() {
  //   return browser.clickLink('Try an example');
  // });
  //
  // it.skip('loads at normal zoom', function(done) {
  //   // browser.assert.elements('.block-item', 234);
  //   this.timeout(15000);
  //   setTimeout(function() {
  //     console.log(browser.query('.block-item').outerHTML);
  //     done()
  //   }, 10000);
  // });
  //
  // it.skip('clicking the plus button increases zoom');
  // it.skip('clicking the minus button decreases zoom');
  // it.skip('clicking the reset button resets zoom');
  // it.skip('clicking the fit button calculates best zoom');
});
